apiVersion: 1
datasources:
    - name: PostgreSQL
      type: postgres
      url: postgres:5432
      database: domus_hub
      user: root
      secureJsonData:
        password: root
      jsonData:
        sslmode: disable
